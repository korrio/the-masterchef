{
  "name": "quiz-masterchef",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ korrio/the-masterchef",
    "create-local": "graph create --node http://localhost:8020/ korrio/the-masterchef",
    "remove-local": "graph remove --node http://localhost:8020/ korrio/the-masterchef",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 korrio/the-masterchef"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
